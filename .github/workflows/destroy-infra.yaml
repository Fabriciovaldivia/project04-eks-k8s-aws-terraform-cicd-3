name: 🧨 Destroy Infrastructure

on:
  workflow_dispatch: # Se activa manualmente desde GitHub Actions

jobs:
  destroy:
    name: 💣 Destruir Infraestructura
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Descargar código
        uses: actions/checkout@v4

      - name: 🔐 Configurar credenciales de AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: us-east-1

      - name: 🔧 Configurar Terraform
        uses: hashicorp/setup-terraform@v3

      - name: 🏗️ Inicializar Terraform
        run: terraform init
        working-directory: ./terraform

      - name: 🗑️ Eliminar Infraestructura (Terraform Destroy)
        run: terraform destroy -auto-approve
        working-directory: ./terraform

      - name: 🗑️ Eliminar Repositorio ECR
        # El repositorio ECR se creó con AWS CLI, así que Terraform no lo borra automáticamente. Lo forzamos aquí.
        run: |
          aws ecr delete-repository --repository-name flask-app --region us-east-1 --force || echo "El repositorio ya no existe"